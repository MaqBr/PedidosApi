# ============================================================
#  PedidosApi — Pipeline CI/CD com Docker Hub + AKS — GitOps
#  Alpha → Beta → RC → Stable
#
#  Parâmetros de execução (apenas no stage Stable):
#    deployStrategy : rolling | blue-green | canary (default: rolling)
#    rollbackTag    : tag da imagem para rollback  (default: vazio)
#                     ex: 1.0.9  →  faz rollback para essa versão
#
#  Estratégias de deploy no Stable:
#    rolling    → deployment.yaml   (RollingUpdate padrão)
#    blue-green → blue-green.yaml   (dois Deployments paralelos)
#    canary     → canary.yaml       (% tráfego para nova versão)
#
#  Rollback:
#    Se rollbackTag for preenchido, o stage Stable ignora o deploy
#    e executa kubectl set image com a tag informada
#
#  Placeholders substituídos via sed antes do kubectl apply:
#    IMAGE_TAG     → versão SemVer ex: 1.2.0
#    NAMESPACE_TAG → alpha | beta | rc | stable
#    ENV_TAG       → Alpha | Beta | RC | Stable
#
#  Manifests aplicados em ordem (dependência):
#    1. configmap.yaml   → variáveis de ambiente
#    2. deployment.yaml  → pods (consome configmap)
#    3. service.yaml     → LoadBalancer
#    4. hpa.yaml         → autoscaling (consome deployment)
#
#  Triggers por tag SemVer:
#    v*.*.*-alpha.*  → CI + Deploy Alpha
#    v*.*.*-beta.*   → CI + Deploy Beta
#    v*.*.*-rc.*     → CI + Deploy RC  (aprovação: Tech Lead)
#    v*.*.*          → CI + Deploy Stable (aprovação: Tech Lead + PM)
#
#  Pré-requisitos:
#    Service connections:
#      sc-dockerhub-pedidosapi → Docker Hub
#      sc-azure-pedidosapi     → Azure Resource Manager
#    Environments: alpha, beta, rc, stable
#    AKS: namespaces e secrets criados previamente
# ============================================================

# ── Parâmetros — visíveis na UI do Azure DevOps ───────────────
parameters:
- name: deployStrategy
  displayName: 'Estratégia de Deploy (apenas Stable)'
  type: string
  default: 'rolling'
  values:
  - rolling
  - blue-green
  - canary

- name: rollbackTag
  displayName: 'Rollback para tag (ex: 1.0.9) — deixe vazio para deploy normal'
  type: string
  default: ''

trigger:
  branches:
    include:
      - main
      - 'feat/*'
      - 'fix/*'
  tags:
    include:
      - 'v*.*.*'

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  dotnetVersion:      '10.x'
  DOCKERHUB_REPO:     'maqbrdevops/pedidosapi'
  AKS_RESOURCE_GROUP: 'DefaultResourceGroup-CCAN'
  AKS_CLUSTER:        'aks-pedidosapi-curso'
  K8S_MANIFESTS_DIR:  'k8s'
  K8S_DEPLOYMENT:     'pedidosapi'

stages:

# ── CI ────────────────────────────────────────────────────────
- stage: CI
  displayName: 'CI — Build, Test e Push Docker Hub'
  # CI é pulado se for rollback — imagem já existe no Docker Hub
  condition: eq('${{ parameters.rollbackTag }}', '')
  jobs:
  - job: BuildTestPush
    displayName: 'Build, Test e Push para Docker Hub'
    steps:

    - checkout: self
      fetchDepth: 0

    - task: UseDotNet@2
      displayName: '1. Instalar .NET SDK $(dotnetVersion)'
      inputs:
        version: '$(dotnetVersion)'
        includePreviewVersions: true

    - script: |
        BRANCH="$(Build.SourceBranch)"
        if [[ "$BRANCH" == refs/tags/v* ]]; then
          VERSION="${BRANCH#refs/tags/v}"
          ASSEMBLY_VERSION=$(echo "$VERSION" | cut -d'-' -f1)
        else
          VERSION="0.0.0-$(Build.BuildNumber)"
          ASSEMBLY_VERSION="0.0.0"
        fi

        if [[ "$BRANCH" == refs/heads/main ]] || [[ "$VERSION" == *-alpha.* ]]; then
          ENV_TAG="alpha"
          ENV_NAME="Alpha"
        elif [[ "$VERSION" == *-beta.* ]]; then
          ENV_TAG="beta"
          ENV_NAME="Beta"
        elif [[ "$VERSION" == *-rc.* ]]; then
          ENV_TAG="rc"
          ENV_NAME="RC"
        else
          ENV_TAG="stable"
          ENV_NAME="Stable"
        fi

        echo "##vso[task.setvariable variable=appVersion;isOutput=true]$VERSION"
        echo "##vso[task.setvariable variable=assemblyVersion]$ASSEMBLY_VERSION"
        echo "##vso[task.setvariable variable=envTag;isOutput=true]$ENV_TAG"
        echo "##vso[task.setvariable variable=envName;isOutput=true]$ENV_NAME"
        echo "Versão: $VERSION | Assembly: $ASSEMBLY_VERSION | Tag: $ENV_TAG | Env: $ENV_NAME"
      name: versao
      displayName: '2. Calcular versão SemVer e tag de ambiente'

    - task: DotNetCoreCLI@2
      displayName: '3. Restaurar pacotes NuGet'
      inputs:
        command: restore
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: '4. Build'
      inputs:
        command: build
        projects: '**/*.csproj'
        arguments: >
          --configuration $(buildConfiguration)
          --no-restore
          -p:Version=$(assemblyVersion)
          -p:AssemblyVersion=$(assemblyVersion)
          -p:FileVersion=$(assemblyVersion)
          -p:InformationalVersion=$(versao.appVersion)
          -p:IncludeSourceRevisionInInformationalVersion=false

    - task: DotNetCoreCLI@2
      displayName: '5. Testes unitários com cobertura'
      inputs:
        command: test
        projects: '**/*Tests*.csproj'
        arguments: >
          --configuration $(buildConfiguration)
          --no-build
          --collect:"XPlat Code Coverage"
        publishTestResults: true

    - task: Docker@2
      displayName: '6. Build e Push da imagem para o Docker Hub'
      inputs:
        command: buildAndPush
        containerRegistry: 'sc-dockerhub-pedidosapi'
        repository: '$(DOCKERHUB_REPO)'
        dockerfile: 'Dockerfile'
        buildContext: '.'
        tags: |
          $(versao.appVersion)
          $(versao.envTag)
        arguments: >
          --build-arg VERSION=$(versao.appVersion)
          --build-arg ASSEMBLY_VERSION=$(assemblyVersion)

    - task: PublishPipelineArtifact@1
      displayName: '7. Publicar manifests k8s como artefato'
      inputs:
        targetPath: '$(K8S_MANIFESTS_DIR)'
        artifact: 'k8s-manifests'
        publishLocation: 'pipeline'

# ── ALPHA ─────────────────────────────────────────────────────
- stage: DeployAlpha
  displayName: 'Alpha — Deploy automático'
  dependsOn: CI
  condition: >
    and(
      succeeded(),
      eq('${{ parameters.rollbackTag }}', ''),
      or(
        eq(variables['Build.SourceBranch'], 'refs/heads/main'),
        contains(variables['Build.SourceBranch'], '-alpha.')
      )
    )
  variables:
    appVersion: $[ stageDependencies.CI.BuildTestPush.outputs['versao.appVersion'] ]
  jobs:
  - deployment: DeployAlpha
    displayName: 'Deploy Alpha → AKS namespace: alpha'
    environment: 'alpha'
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo "appVersion=$(appVersion)"
            displayName: '0. Debug appVersion'

          - task: DownloadPipelineArtifact@2
            displayName: '1. Download manifests k8s'
            inputs:
              artifactName: 'k8s-manifests'
              targetPath: '$(Pipeline.Workspace)/k8s'

          - task: AzureCLI@2
            displayName: '2. Substituir placeholders nos manifests'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                echo "→ Substituindo placeholders para namespace: alpha"
                sed -i "s|NAMESPACE_TAG|alpha|g"        $(Pipeline.Workspace)/k8s/configmap.yaml
                sed -i "s|NAMESPACE_TAG|alpha|g"        $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|NAMESPACE_TAG|alpha|g"        $(Pipeline.Workspace)/k8s/service.yaml
                sed -i "s|NAMESPACE_TAG|alpha|g"        $(Pipeline.Workspace)/k8s/hpa.yaml
                sed -i "s|IMAGE_TAG|$(appVersion)|g"    $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|ENV_TAG|Alpha|g"              $(Pipeline.Workspace)/k8s/configmap.yaml

                echo "→ Verificando substituições:"
                grep "namespace:"  $(Pipeline.Workspace)/k8s/deployment.yaml
                grep "image:"      $(Pipeline.Workspace)/k8s/deployment.yaml
                grep "ASPNETCORE_ENVIRONMENT" $(Pipeline.Workspace)/k8s/configmap.yaml

          - task: AzureCLI@2
            displayName: '3. Apply manifests + rollout (alpha)'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                echo "→ Aplicando manifests em ordem no namespace alpha..."
                kubectl apply -f $(Pipeline.Workspace)/k8s/configmap.yaml  -n alpha
                kubectl apply -f $(Pipeline.Workspace)/k8s/deployment.yaml -n alpha
                kubectl apply -f $(Pipeline.Workspace)/k8s/service.yaml    -n alpha
                kubectl apply -f $(Pipeline.Workspace)/k8s/hpa.yaml        -n alpha

                echo "→ Aguardando rollout..."
                kubectl rollout status deployment/$(K8S_DEPLOYMENT) \
                  -n alpha --timeout=300s

          - task: AzureCLI@2
            displayName: '4. Smoke test Alpha'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                sleep 10
                SVC_IP=$(kubectl get svc pedidosapi-svc -n alpha \
                  -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                echo "Service IP: $SVC_IP"
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$SVC_IP/info)
                echo "Smoke test Alpha: HTTP $STATUS"
                [ "$STATUS" = "200" ] && echo "✅ Alpha OK" || exit 1

# ── BETA ──────────────────────────────────────────────────────
- stage: DeployBeta
  displayName: 'Beta — Deploy automático'
  dependsOn: CI
  condition: >
    and(
      succeeded(),
      eq('${{ parameters.rollbackTag }}', ''),
      contains(variables['Build.SourceBranch'], '-beta.')
    )
  variables:
    appVersion: $[ stageDependencies.CI.BuildTestPush.outputs['versao.appVersion'] ]
  jobs:
  - deployment: DeployBeta
    displayName: 'Deploy Beta → AKS namespace: beta'
    environment: 'beta'
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo "appVersion=$(appVersion)"
            displayName: '0. Debug appVersion'

          - task: DownloadPipelineArtifact@2
            displayName: '1. Download manifests k8s'
            inputs:
              artifactName: 'k8s-manifests'
              targetPath: '$(Pipeline.Workspace)/k8s'

          - task: AzureCLI@2
            displayName: '2. Substituir placeholders nos manifests'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                echo "→ Substituindo placeholders para namespace: beta"
                sed -i "s|NAMESPACE_TAG|beta|g"         $(Pipeline.Workspace)/k8s/configmap.yaml
                sed -i "s|NAMESPACE_TAG|beta|g"         $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|NAMESPACE_TAG|beta|g"         $(Pipeline.Workspace)/k8s/service.yaml
                sed -i "s|NAMESPACE_TAG|beta|g"         $(Pipeline.Workspace)/k8s/hpa.yaml
                sed -i "s|IMAGE_TAG|$(appVersion)|g"    $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|ENV_TAG|Beta|g"               $(Pipeline.Workspace)/k8s/configmap.yaml

                echo "→ Verificando substituições:"
                grep "namespace:"  $(Pipeline.Workspace)/k8s/deployment.yaml
                grep "image:"      $(Pipeline.Workspace)/k8s/deployment.yaml
                grep "ASPNETCORE_ENVIRONMENT" $(Pipeline.Workspace)/k8s/configmap.yaml

          - task: AzureCLI@2
            displayName: '3. Apply manifests + rollout (beta)'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                echo "→ Aplicando manifests em ordem no namespace beta..."
                kubectl apply -f $(Pipeline.Workspace)/k8s/configmap.yaml  -n beta
                kubectl apply -f $(Pipeline.Workspace)/k8s/deployment.yaml -n beta
                kubectl apply -f $(Pipeline.Workspace)/k8s/service.yaml    -n beta
                kubectl apply -f $(Pipeline.Workspace)/k8s/hpa.yaml        -n beta

                echo "→ Aguardando rollout..."
                kubectl rollout status deployment/$(K8S_DEPLOYMENT) \
                  -n beta --timeout=300s

          - task: AzureCLI@2
            displayName: '4. Smoke test Beta'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                sleep 10
                SVC_IP=$(kubectl get svc pedidosapi-svc -n beta \
                  -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                echo "Service IP: $SVC_IP"
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$SVC_IP/info)
                echo "Smoke test Beta: HTTP $STATUS"
                [ "$STATUS" = "200" ] && echo "✅ Beta OK" || exit 1

# ── RC ────────────────────────────────────────────────────────
- stage: DeployRC
  displayName: 'RC — Aprovação obrigatória'
  dependsOn: CI
  condition: >
    and(
      succeeded(),
      eq('${{ parameters.rollbackTag }}', ''),
      contains(variables['Build.SourceBranch'], '-rc.')
    )
  variables:
    appVersion: $[ stageDependencies.CI.BuildTestPush.outputs['versao.appVersion'] ]
  jobs:
  - deployment: DeployRC
    displayName: 'Deploy RC → AKS namespace: rc'
    environment: 'rc'
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo "appVersion=$(appVersion)"
            displayName: '0. Debug appVersion'

          - task: DownloadPipelineArtifact@2
            displayName: '1. Download manifests k8s'
            inputs:
              artifactName: 'k8s-manifests'
              targetPath: '$(Pipeline.Workspace)/k8s'

          - task: AzureCLI@2
            displayName: '2. Substituir placeholders nos manifests'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                echo "→ Substituindo placeholders para namespace: rc"
                sed -i "s|NAMESPACE_TAG|rc|g"           $(Pipeline.Workspace)/k8s/configmap.yaml
                sed -i "s|NAMESPACE_TAG|rc|g"           $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|NAMESPACE_TAG|rc|g"           $(Pipeline.Workspace)/k8s/service.yaml
                sed -i "s|NAMESPACE_TAG|rc|g"           $(Pipeline.Workspace)/k8s/hpa.yaml
                sed -i "s|IMAGE_TAG|$(appVersion)|g"    $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|ENV_TAG|RC|g"                 $(Pipeline.Workspace)/k8s/configmap.yaml

                echo "→ Verificando substituições:"
                grep "namespace:"  $(Pipeline.Workspace)/k8s/deployment.yaml
                grep "image:"      $(Pipeline.Workspace)/k8s/deployment.yaml
                grep "ASPNETCORE_ENVIRONMENT" $(Pipeline.Workspace)/k8s/configmap.yaml

          - task: AzureCLI@2
            displayName: '3. Apply manifests + rollout (rc)'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                echo "→ Aplicando manifests em ordem no namespace rc..."
                kubectl apply -f $(Pipeline.Workspace)/k8s/configmap.yaml  -n rc
                kubectl apply -f $(Pipeline.Workspace)/k8s/deployment.yaml -n rc
                kubectl apply -f $(Pipeline.Workspace)/k8s/service.yaml    -n rc
                kubectl apply -f $(Pipeline.Workspace)/k8s/hpa.yaml        -n rc

                echo "→ Aguardando rollout..."
                kubectl rollout status deployment/$(K8S_DEPLOYMENT) \
                  -n rc --timeout=300s

          - task: AzureCLI@2
            displayName: '4. Smoke test RC'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                sleep 10
                SVC_IP=$(kubectl get svc pedidosapi-svc -n rc \
                  -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                echo "Service IP: $SVC_IP"
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$SVC_IP/info)
                echo "Smoke test RC: HTTP $STATUS"
                [ "$STATUS" = "200" ] && echo "✅ RC OK" || exit 1

# ── STABLE ────────────────────────────────────────────────────
# Este stage suporta 3 modos:
#   1. Deploy normal    → rollbackTag vazio  + deployStrategy = rolling
#   2. Deploy avançado  → rollbackTag vazio  + deployStrategy = blue-green | canary
#   3. Rollback         → rollbackTag = tag  → ignora deploy, restaura versão anterior
- stage: DeployStable
  displayName: 'Stable — Produção (aprovação dupla)'
  dependsOn:
  - CI
  condition: >
    and(
      in(dependencies.CI.result, 'Succeeded', 'Skipped'),
      or(
        ne('${{ parameters.rollbackTag }}', ''),
        and(
          startsWith(variables['Build.SourceBranch'], 'refs/tags/v'),
          not(contains(variables['Build.SourceBranch'], '-alpha.')),
          not(contains(variables['Build.SourceBranch'], '-beta.')),
          not(contains(variables['Build.SourceBranch'], '-rc.'))
        )
      )
    )
  variables:
    appVersion: $[ stageDependencies.CI.BuildTestPush.outputs['versao.appVersion'] ]
  jobs:

  # ── JOB: Rollback ─────────────────────────────────────────
  # Executado SOMENTE quando rollbackTag for preenchido
  # Restaura a imagem para a tag informada sem rebuild
  - deployment: RollbackStable
    displayName: 'Rollback Stable → tag: ${{ parameters.rollbackTag }}'
    condition: ne('${{ parameters.rollbackTag }}', '')
    environment: 'stable'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureCLI@2
            displayName: '1. Rollback para tag ${{ parameters.rollbackTag }}'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                ROLLBACK_TAG="${{ parameters.rollbackTag }}"
                echo "⏪ Iniciando rollback para: $(DOCKERHUB_REPO):$ROLLBACK_TAG"

                # Verifica se a imagem existe no Docker Hub antes de aplicar
                if docker manifest inspect $(DOCKERHUB_REPO):$ROLLBACK_TAG > /dev/null 2>&1; then
                  echo "✅ Imagem encontrada no Docker Hub"
                else
                  echo "❌ Imagem $(DOCKERHUB_REPO):$ROLLBACK_TAG não encontrada no Docker Hub!"
                  exit 1
                fi

                kubectl set image deployment/$(K8S_DEPLOYMENT) \
                  $(K8S_DEPLOYMENT)=$(DOCKERHUB_REPO):$ROLLBACK_TAG \
                  -n stable

                echo "→ Aguardando rollback..."
                kubectl rollout status deployment/$(K8S_DEPLOYMENT) \
                  -n stable --timeout=300s

          - task: AzureCLI@2
            displayName: '2. Smoke test pós-rollback'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                sleep 10
                SVC_IP=$(kubectl get svc pedidosapi-svc -n stable \
                  -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$SVC_IP/info)
                echo "Smoke test pós-rollback: HTTP $STATUS"
                [ "$STATUS" = "200" ] && echo "✅ Rollback concluído!" || exit 1

          - script: |
              echo "================================================"
              echo " ⏪ ROLLBACK CONCLUÍDO"
              echo " Versão restaurada : ${{ parameters.rollbackTag }}"
              echo " Imagem            : $(DOCKERHUB_REPO):${{ parameters.rollbackTag }}"
              echo " Namespace         : stable"
              echo "================================================"
            displayName: '3. Resumo do rollback'

  # ── JOB: Deploy Rolling ───────────────────────────────────
  # Estratégia padrão — deployment.yaml com RollingUpdate
  - deployment: DeployRolling
    displayName: 'Deploy Stable — Rolling Update'
    condition: >
      and(
        eq('${{ parameters.rollbackTag }}', ''),
        eq('${{ parameters.deployStrategy }}', 'rolling')
      )
    environment: 'stable'
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo "appVersion=$(appVersion)"
            displayName: '0. Debug appVersion'

          - task: DownloadPipelineArtifact@2
            displayName: '1. Download manifests k8s'
            inputs:
              artifactName: 'k8s-manifests'
              targetPath: '$(Pipeline.Workspace)/k8s'

          - task: AzureCLI@2
            displayName: '2. Substituir placeholders nos manifests'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                sed -i "s|NAMESPACE_TAG|stable|g"       $(Pipeline.Workspace)/k8s/configmap.yaml
                sed -i "s|NAMESPACE_TAG|stable|g"       $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|NAMESPACE_TAG|stable|g"       $(Pipeline.Workspace)/k8s/service.yaml
                sed -i "s|NAMESPACE_TAG|stable|g"       $(Pipeline.Workspace)/k8s/hpa.yaml
                sed -i "s|IMAGE_TAG|$(appVersion)|g"    $(Pipeline.Workspace)/k8s/deployment.yaml
                sed -i "s|ENV_TAG|Stable|g"             $(Pipeline.Workspace)/k8s/configmap.yaml

                grep "namespace:"  $(Pipeline.Workspace)/k8s/deployment.yaml
                grep "image:"      $(Pipeline.Workspace)/k8s/deployment.yaml

          - task: AzureCLI@2
            displayName: '3. Apply Rolling + rollout (stable)'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                kubectl apply -f $(Pipeline.Workspace)/k8s/configmap.yaml  -n stable
                kubectl apply -f $(Pipeline.Workspace)/k8s/deployment.yaml -n stable
                kubectl apply -f $(Pipeline.Workspace)/k8s/service.yaml    -n stable
                kubectl apply -f $(Pipeline.Workspace)/k8s/hpa.yaml        -n stable

                kubectl rollout status deployment/$(K8S_DEPLOYMENT) \
                  -n stable --timeout=360s

          - task: AzureCLI@2
            displayName: '4. Smoke test Stable'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                sleep 10
                SVC_IP=$(kubectl get svc pedidosapi-svc -n stable \
                  -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$SVC_IP/info)
                echo "Smoke test Stable Rolling: HTTP $STATUS"
                [ "$STATUS" = "200" ] && echo "✅ Stable Rolling OK" || exit 1

          - script: |
              echo "================================================"
              echo " ✅ DEPLOY ROLLING CONCLUÍDO"
              echo " Versão  : $(appVersion)"
              echo " Imagem  : $(DOCKERHUB_REPO):$(appVersion)"
              echo " NS      : stable"
              echo "================================================"
            displayName: '5. Resumo do deploy'

  # ── JOB: Deploy Blue-Green ────────────────────────────────
  # Aplica blue-green.yaml — dois Deployments paralelos (blue e green)
  # O Service aponta para 'blue' por padrão — troca manual após validação
  - deployment: DeployBlueGreen
    displayName: 'Deploy Stable — Blue-Green'
    condition: >
      and(
        eq('${{ parameters.rollbackTag }}', ''),
        eq('${{ parameters.deployStrategy }}', 'blue-green')
      )
    environment: 'stable'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadPipelineArtifact@2
            displayName: '1. Download manifests k8s'
            inputs:
              artifactName: 'k8s-manifests'
              targetPath: '$(Pipeline.Workspace)/k8s'

          # No blue-green, a nova versão vai para o Green
          # A imagem do Blue (versão atual) fica intacta no manifest
          # Apenas o Green recebe IMAGE_TAG da nova versão
          - task: AzureCLI@2
            displayName: '2. Substituir IMAGE_TAG no Green'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                echo "→ Estratégia: Blue-Green"
                echo "→ Blue  = versão atual (já rodando)"
                echo "→ Green = nova versão: $(appVersion)"

                # sed substitui os dois placeholders:
                #   BLUE_TAG  → versão atual em produção (obtida do Deployment blue em execução)
                #   IMAGE_TAG → nova versão (appVersion calculado no CI)
                CURRENT_BLUE=$(kubectl get deployment pedidosapi-blue -n stable \
                  -o jsonpath='{.spec.template.spec.containers[0].image}' \
                  2>/dev/null | cut -d: -f2 || echo "latest")
                echo "→ Blue atual: $CURRENT_BLUE | Green novo: $(appVersion)"

                sed -i "s|BLUE_TAG|$CURRENT_BLUE|g"        $(Pipeline.Workspace)/k8s/blue-green.yaml
                sed -i "s|IMAGE_TAG|$(appVersion)|g"        $(Pipeline.Workspace)/k8s/blue-green.yaml

                echo "→ Imagens no blue-green.yaml após substituição:"
                grep "image:" $(Pipeline.Workspace)/k8s/blue-green.yaml

          - task: AzureCLI@2
            displayName: '3. Apply Blue-Green + aguardar Green'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                kubectl apply -f $(Pipeline.Workspace)/k8s/configmap.yaml    -n stable
                kubectl apply -f $(Pipeline.Workspace)/k8s/blue-green.yaml   -n stable

                echo "→ Aguardando Green subir..."
                kubectl rollout status deployment/pedidosapi-green \
                  -n stable --timeout=360s

                echo ""
                echo "🔵 Blue  (versão atual) → tráfego 100%"
                echo "🟢 Green (nova versão)  → pronto, aguardando switch"
                echo ""
                echo "Para alternar o tráfego para Green execute:"
                echo "kubectl patch svc pedidosapi-svc -n stable -p '{\"spec\":{\"selector\":{\"version\":\"green\"}}}'"

          - task: AzureCLI@2
            displayName: '4. Smoke test Green (sem tráfego ainda)'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                sleep 10
                # Smoke test via ClusterIP do Green (pedidosapi-green-svc)
                GREEN_IP=$(kubectl get svc pedidosapi-green-svc -n stable \
                  -o jsonpath='{.spec.clusterIP}')
                echo "Green ClusterIP: $GREEN_IP"

                # Roda o teste dentro do cluster via pod temporário
                kubectl run smoke-test --rm -i --restart=Never \
                  --image=curlimages/curl:latest -n stable -- \
                  curl -s -o /dev/null -w "%{http_code}" http://$GREEN_IP/info \
                  | grep -q "200" && echo "✅ Green OK — pronto para switch!" || exit 1

  # ── JOB: Deploy Canary ────────────────────────────────────
  # Aplica canary.yaml — 9 pods stable + 1 pod canary = 10% tráfego
  # Após validação: aumentar réplicas canary ou promover para stable
  - deployment: DeployCanary
    displayName: 'Deploy Stable — Canary (10% tráfego)'
    condition: >
      and(
        eq('${{ parameters.rollbackTag }}', ''),
        eq('${{ parameters.deployStrategy }}', 'canary')
      )
    environment: 'stable'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadPipelineArtifact@2
            displayName: '1. Download manifests k8s'
            inputs:
              artifactName: 'k8s-manifests'
              targetPath: '$(Pipeline.Workspace)/k8s'

          - task: AzureCLI@2
            displayName: '2. Substituir IMAGE_TAG no Canary'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                echo "→ Estratégia: Canary"
                echo "→ Stable = 9 pods (versão atual) = 90% tráfego"
                echo "→ Canary = 1 pod  (nova versão)  = 10% tráfego"

                # sed substitui os dois placeholders:
                #   STABLE_TAG → versão atual em produção (obtida do Deployment stable em execução)
                #   IMAGE_TAG  → nova versão canary (appVersion calculado no CI)
                CURRENT_STABLE=$(kubectl get deployment pedidosapi -n stable \
                  -o jsonpath='{.spec.template.spec.containers[0].image}' \
                  2>/dev/null | cut -d: -f2 || echo "latest")
                echo "→ Stable atual: $CURRENT_STABLE | Canary novo: $(appVersion)"

                sed -i "s|STABLE_TAG|$CURRENT_STABLE|g"    $(Pipeline.Workspace)/k8s/canary.yaml
                sed -i "s|IMAGE_TAG|$(appVersion)|g"        $(Pipeline.Workspace)/k8s/canary.yaml

                echo "→ Imagens no canary.yaml após substituição:"
                grep "image:" $(Pipeline.Workspace)/k8s/canary.yaml

          - task: AzureCLI@2
            displayName: '3. Apply Canary + aguardar pod canary'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                kubectl apply -f $(Pipeline.Workspace)/k8s/configmap.yaml -n stable
                kubectl apply -f $(Pipeline.Workspace)/k8s/canary.yaml    -n stable

                echo "→ Aguardando pod canary subir..."
                kubectl rollout status deployment/pedidosapi-canary \
                  -n stable --timeout=360s

                echo ""
                echo "📊 Distribuição de tráfego:"
                kubectl get deployments -n stable | grep -E "pedidosapi|NAME"
                echo ""
                echo "Para promover canary para 100%:"
                echo "kubectl scale deployment/pedidosapi --replicas=0 -n stable"
                echo "kubectl scale deployment/pedidosapi-canary --replicas=10 -n stable"
                echo ""
                echo "Para rollback do canary:"
                echo "kubectl delete deployment pedidosapi-canary -n stable"

          - task: AzureCLI@2
            displayName: '4. Smoke test Canary'
            inputs:
              azureSubscription: 'sc-azure-pedidosapi'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(AKS_RESOURCE_GROUP) \
                  --name $(AKS_CLUSTER) \
                  --overwrite-existing

                sleep 10
                SVC_IP=$(kubectl get svc pedidosapi-svc -n stable \
                  -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$SVC_IP/info)
                echo "Smoke test Canary: HTTP $STATUS"
                [ "$STATUS" = "200" ] && echo "✅ Canary ativo — monitorar métricas!" || exit 1
